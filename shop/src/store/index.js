import Vue from 'vue'
import { createStore } from 'vuex'

import banner from './banner/index';

export default createStore({
  modules: {
    banner
  },
  state: {
  },
  mutations: {
  },
  actions: {
  }
})
